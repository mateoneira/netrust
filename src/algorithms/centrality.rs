use crate::algorithms::shortest_path::dijsktra;
use crate::graph::Graph;
use std::collections::{HashMap, HashSet};

pub fn closeness(graph: &Graph) -> HashMap<usize, f32> {
    let mut closeness = HashMap::new();
}

pub fn betweenness(graph: &Graph) -> HashMap<usize, f32> {
    HashMap::new()
}
